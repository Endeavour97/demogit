---

 - hosts: centos
   become: yes
   
   tasks:
     - name: Install epel
       shell: sudo amazon-linux-extras install epel -y
 
     - name: Enable epel
       shell: sudo yum-config-manager --enable epel
